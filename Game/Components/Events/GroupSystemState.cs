namespace Game;

struct GroupSystemState
{
    public string name;

    public bool state;

    public GroupSystemState(string name, bool state)
    {
        this.name = name;
        this.state = state;
    }
}